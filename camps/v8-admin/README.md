# v8-admin

V8 platform admin agent camp — users, credits, verses, comments, game payments, analytics.

## Skills

- **v8-admin** — V8 platform admin API operations via GraphQL

## Install

```bash
curl -fsSL https://raw.githubusercontent.com/planetarium/CampForge/main/camps/v8-admin/install.sh | bash
```

Set a custom workspace:

```bash
WORKSPACE=/path/to/workspace curl -fsSL https://raw.githubusercontent.com/planetarium/CampForge/main/camps/v8-admin/install.sh | bash
```

## Prerequisites

- `gq` (graphqurl): `npm i -g graphqurl`

## Validate

```bash
campforge validate .
```

---

Generated by [CampForge](https://github.com/planetarium/CampForge)
